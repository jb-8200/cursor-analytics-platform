Display the specification for the requested service.

If I specify a service name (cursor-sim, cursor-analytics-core, or cursor-viz-spa):
- Read services/{service-name}/SPEC.md
- Show key sections: Overview, API Endpoints, Data Models, Configuration

If no service specified:
- List available services
- Show brief description of each
