# Database Configuration
# PostgreSQL connection string for cursor-analytics-core
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public

# Development database (local PostgreSQL)
DATABASE_URL="postgresql://cursor:cursor_dev@localhost:5432/cursor_analytics?schema=public"

# Note: For production, use environment-specific credentials
# Prisma will use this URL for migrations and client connections
